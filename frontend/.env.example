# Backend API base URL. Replace with your deployed backend URL when deploying.
# Leave empty in development if using Vite proxy (default: /api -> http://localhost:3001).
VITE_API_URL=http://localhost:3001
